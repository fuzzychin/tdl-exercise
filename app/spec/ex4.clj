(ns app.core-spec
  (:require [speclj.core :refer :all]
            [app.core :refer :all]))

(describe "===Ex 4===")
(describe "===Data Structures===")
(describe "Maps"
  (it "works like a dictionary and i can add/remove values"
    ))

(describe "Vectors"
  (it "Write some tests on vector operations"
    ))
(describe "Lsts"
  (it "works like a clojure List"
    ))
(describe "Sets"
  (it "works like a clojure Set"
    ))
(describe "Destructuring"
  (it "[pg68]"
    ))
