(ns app.core-spec
  (:require [speclj.core :refer :all]
            [app.core :refer :all]
            [clojure.string :refer :all]))

(describe "the joy of strings"
  (it "FIXME, I fail."
    (should= true (blank? ""))))
